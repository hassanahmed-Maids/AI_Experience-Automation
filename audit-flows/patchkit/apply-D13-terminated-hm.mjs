// D13 — Terminated Housemaids: the Ex reference is the pairing authority, not the amount.
//
//   node apply-D13-terminated-hm.mjs <offline scorer.js>
//
// Business rule, Alert 648's owner: "each transaction with amount < 0 cancels one
// transaction". The old magnitude tolerance rejected maid 29463's 1,306.20 / -1,035.00
// pair (271.20 residue against a 130.62 tolerance), so the negative was consumed, the
// positive scored in full, and the duplicate-destination list read 8 maids where the
// spec's corrected figure is 7. Widening the tolerance was the WRONG fix: the floor's own
// note records the smallest real charge at AED 194.22, so a tolerance wide enough for
// 271.20 would start netting away genuine second tickets.
import { apply } from './_lib.mjs';

const file = process.argv[2];
if (!file) { console.error('usage: node apply-D13-terminated-hm.mjs <path to the offline scorer>'); process.exit(1); }

apply(file, [
  { name: 'reference-authoritative netting (magnitude demoted to tiebreaker)', find: "      const tol = Math.max(FEE_RESIDUAL_FLOOR, best.amount * 0.10);\n      if (bestDiff <= tol) {\n        claimed.add(best);\n        best._netted = true;\n        best._net_reason = 'reversed_by_' + n.transaction_id;\n        if (bestDiff > 0.005) best._fee_retained = Number(bestDiff.toFixed(2));\n      } else {\n        n._net_reason = 'reversal_magnitude_unmatched_in_group';\n      }", replace: "      // D13, 2026-08-26. THE Ex REFERENCE IS THE PAIRING AUTHORITY, NOT THE AMOUNT.\n      // The business rule is count-based - Alert 648's owner: \"each transaction with\n      // amount < 0 cancels one transaction\" - so inside one reference group a negative\n      // cancels a positive outright and whatever is left is a retained fee, however big.\n      //\n      // The old tolerance, max(FEE_RESIDUAL_FLOOR, best.amount * 0.10), rejected maid\n      // 29463's pair: a 1,306.20 charge against a 1,035.00 reversal is a 271.20 residue\n      // against a 130.62 tolerance. The negative was consumed and the positive scored in\n      // full, so she read as two tickets to Sri Lanka instead of one and the duplicate\n      // list showed 8 maids where the spec's corrected figure is 7.\n      //\n      // Widening the tolerance was the WRONG fix and is why this is written out: the\n      // floor's own note records the smallest real charge at AED 194.22, so any tolerance\n      // wide enough for 271.20 would start netting away genuine second tickets - trading\n      // one false duplicate for a false clearance, which is the expensive direction.\n      // Magnitude keeps only the job it can do safely, choosing WHICH positive a negative\n      // pairs with when a group holds several, and never whether they pair at all.\n      claimed.add(best);\n      best._netted = true;\n      best._net_reason = 'reversed_by_' + n.transaction_id;\n      if (bestDiff > 0.005) best._fee_retained = Number(bestDiff.toFixed(2));\n      // Recorded, never rejected. A residue above the fee floor is still a valid pair\n      // under the count rule, but it is unusual enough that the run should say so rather\n      // than absorb it silently.\n      if (bestDiff > Math.max(FEE_RESIDUAL_FLOOR, best.amount * 0.10)) {\n        best._fee_retained_above_floor = true;\n      }" },
  { name: 'group note carries the above-floor retained-fee count', find: "    notes.push({ ex_ref: ex, maid_id: maidId, rows_in_group: universe.length,\n      out_of_window_counterparts: universe.filter(function (r) { return r._out_of_window; }).length,\n      netted: universe.filter(function (r) { return r._netted; }).length });", replace: "    notes.push({ ex_ref: ex, maid_id: maidId, rows_in_group: universe.length,\n      out_of_window_counterparts: universe.filter(function (r) { return r._out_of_window; }).length,\n      netted: universe.filter(function (r) { return r._netted; }).length,\n      // D13: surfaces a pair whose retained fee is larger than the fee floor expects.\n      // Valid under the count rule, worth a reader's eye.\n      fee_retained_above_floor: universe.filter(function (r) { return r._fee_retained_above_floor; }).length });" },
], 'D13');
