# Deployment ticket drafts — 2026-08-30

**Nothing here has been posted.** Six drafts, generated by `/draft-deploy`, awaiting review.

All six are written to the **2026-08-30 architecture ruling**: scheduled monthly, no webhook in or
out, results to Google Sheets. None of the flows has been converted yet — each draft carries a
**PRE-DEPLOYMENT CONVERSION REQUIRED** block naming exactly what it needs. Full spec:
`../../records/webhook-to-schedule-conversion.md`.

| Draft | Dept | Conversion needed | Measured run | Verdict |
|---|---|---|---|---|
| `wellcare-medical-loan.md` | Money Control | **add a trigger — that's all** | 264 s (2026-08-25) | smallest conversion in the batch |
| `cc-overstay-fines.md` | Money Control | trigger + delete 2 callbacks | 33 s (2026-08-24) | ⚠ staging ERP token wired |
| `dummy-tickets.md` | Money Control | swap trigger, delete 3 callbacks + 2 respond | 238 s (2026-08-24) | ⚠ **published — unpublish before rewiring** |
| `mv-overstay-fines.md` | Money Collection | swap trigger, delete 3 callbacks + 2 respond | ⚠ **none — last 5 runs failed** | fix the failures first |
| `applicant-real-ticket.md` | Money Control | ⚠ **build the Sheets output — new work** | 273 s (2026-08-19) | zero Sheets nodes today |
| `mv-monthly-payment.md` | Money Collection | remove 2 webhooks; loses the rollup recovery path | — | ⛔ **four stop conditions — do not post** |

## What the generation turned up

Things the drafts surface that were not visible from the Notion queue:

- **Two flows have no trigger node at all** (Wellcare, CC Overstay Fines). Every recorded execution
  ran in `manual` mode — which, under the schedule ruling, makes Wellcare the *easiest* conversion
  rather than the most broken: it needs a trigger added and nothing removed.

- **Applicant Real Ticket has no Google Sheets output at all.** Zero Sheets nodes, no callback
  either — its results live only in its five Data Tables, so today nothing leaves the flow. Under
  the ruling that results are posted to Google Sheets, this is new build work, not a rewire. It is
  the only one of the six where that is true.
- **MV Overstay Fines has no successful run.** Eleven executions; the five most recent are all
  `error` or `canceled`. A likely cause is the staging ERP token it holds (`ERP Token 12th Aug
  2026`) having expired — worth checking first, but the failures still have to be resolved before
  this deploys, whatever the cause.
- **Dummy Tickets is the only published flow**, with a live production webhook, while every other
  check in the batch is an unpublished draft.
- **Applicant Real Ticket has a results sheet in Notion but no Sheets node wired** on the workflow.
  The credential to use is **Hassan Maids Account**; the wiring still needs confirming.

- **Two flows hold a staging ERP token** (`ERP Token 12th Aug 2026` — CC Overstay Fines and MV
  Overstay Fines). A staging token must never travel to production inside an export. The intended
  state is *no* ERP credential in the flow, with the deploying team creating one from a production
  token — which is how every draft now words the ask.
- **Every one of the six calls at least one banned route.** `advancesearchNew` is Section A (no
  alternative) on five of them; MV Overstay Fines additionally calls
  `/accounting/payments/page/advancesearch`, the endpoint recorded as having taken Accounting ERP
  down once — and a swap for it already exists and is in use on MV Monthly Payment Stage 2.
- **MV Monthly Payment should not be in this batch.** Stage 0 is marked UNTESTED by its author,
  exists because this check caused the 2026-08-19 `clientmgmt` outage, and still calls the endpoint
  implicated in it.

## Before any of these are posted

0. **Apply the webhook→schedule conversions.** No flow matches the ruling yet, so no ticket
   describes a flow that exists. Confirm the cadence (proposed: 15th, 06:00 Asia/Dubai, previous
   full month), unpublish Dummy Tickets first, and agree Applicant Real Ticket's sheet layout.
1. Resolve the route-ban posture (disclose / hold / swap) — every draft carries a
   **Known route exceptions** section written for the disclose option.
2. Fill the TODOs. The two that recur: the ERP request count per run, and the notification
   recipient. Neither is derivable from the workflow JSON.
3. Decide the production trigger for Wellcare and CC Overstay Fines.
4. Post with `contentFormat: "markdown"`, then read back with `responseContentFormat: "adf"` and
   confirm a `doc` object — see `../README.md`.
