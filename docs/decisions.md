# Decisions log (append-only)

Format: `YYYY-MM-DD — decision — why`. Newest at top. Log every judgment call that future runs should not re-litigate.

- 2026-07-20 — **Non-event REBUILD shipped as new draft campaigns 120–123 (env 216662).** Running event campaigns 113/114/117 could not be converted in place (`write:live` scope never propagated to the connector token), so per Hassan we rebuilt as new drafts (no scope needed) and will retire 113/114/117 at cutover. All four: `type=relationship`, `relationship_campaign{object_type_id:1 (Contract), type:2}`, `restart_mode=rematch`, draft, single ERP-send webhook→exit, all sends `sending_state=draft`. Encoding gotcha: `relationship_attribute_triggers` must be sent as a **base64(urlencode(JSON)) STRING** (arrays are 400-rejected); conditions use `type:"relationship_attribute", type_id:1`. Campaigns: **120** Cancellation `[rel]` (client + status=CANCELLED + contract_type=mv + has_cancellation_paper + cancelled_within_60d; params `maid_name`/`cancellation_paper_link` via `{{trigger.relationship.<attr>}}`); **121** Maid-Under-24 `[rel]` (ACTIVE + mv + maid_nationality=philippines + maid_under_24; params `{}`, ERP resolves); **122** Auto-Updates-Enabled `[attr]` + **123** Auto-Updates-Disabled `[attr]` (split so `auto_updates_last_action`=enabled/disabled fires each transition and the default never-toggled population — which defaults to enabled — is never messaged; static templates, params `{}`). **REQUIRED SYNC-ADDS (data-team, on the Contract relationship, type_id 1) before any of these match anyone:** `contract_type`(exists but confirm value `mv`), `has_cancellation_paper`(bool), `cancelled_within_60d`(bool), `maid_nationality`, `maid_under_24`(bool, precomputed from birthdate), `auto_updates_last_action`(enabled/disabled, set per toggle), `maid_name`, `cancellation_paper_link`(built from the persisted attachment UUID). Until synced, all four match 0 — expected.
- 2026-07-20 — **Non-event redesign — Snowflake validation + real message bodies (Hassan ran the queries).** Fact table `BROADCASTS_FINAL_LAYER` carries `BROADCAST_CONTENT` (rendered) + `TEMPLATE_CONTENT` (with @params@). Findings: **(1) Flow 3 templates are STATIC** — `CM_DISABLE_AUTO_UPDATES` = "Your auto-updates have been successfully disabled. If you wish to re-enable them, please let us know!" with **zero @params@** → the auto-updates sends need NO link params (the earlier ccapp_link/accommodation concern is moot; supersedes that part of the auto_updates build spec). **(2) Flow 1 (8812)** body confirms params `@maid_name@`+`@cancellation_paper_link@` (sent link is short.gy-shortened). Relationship-trigger validation: 285 recent recipients → 224 latest-cancelled / 210 within-60d; the ~21% gap is because the query used *client's* latest contract while the real CIO relationship trigger is **per-contract** (any matching CANCELLED contract) — so drop the "latest contract" restriction from the trigger; it matches better without it. **(3) Flow 2 (`mv_maid_under_24`) and `CM_ENABLE_AUTO_UPDATES` have 0 sends in 45 days** — very low volume, no recent population to validate against (validate over a wider window; bodies not fetchable from recent sends). Doc updated with the real bodies.
- 2026-07-20 — **STANDING RULE (Hassan): avoid Event triggers — exhaust Attribute / Relationship / Object triggers first.** Do the work to trigger on PERSISTED, query-able state, creating whatever is needed (sync-adds, new attributes, relationships, computed flags) to make a non-event trigger feasible. Only fall back to an Event when the trigger truly cannot be expressed as persisted state (and justify why). This strengthens the existing trigger-preference order in `customerio-conventions.md` (which already ranks relationship/state above event) into a hard "no event unless proven impossible." Additional standing rules: (1) **for every WhatsApp template, fetch the real message body and put it in the documentation** (board note / mmdb `TEMPLATES`+LivePerson / a real Snowflake send) — don't leave "message: <description>" placeholders; (2) **produce Snowflake queries Hassan runs himself** to pull real examples of the data that currently triggers the flow on ERP, then validate that the redesigned persisted-state trigger would actually match that population before calling a flow done. Applied by refactoring campaigns 113/114/117 off events (see per-flow entries).

- 2026-07-20 — **Liquid namespace for event-triggered campaigns is `{{event.<field>}}`, NOT `{{trigger.*}}` (clarified erp-send-webhook.md).** CIO's `recipes/liquid_syntax.md` is explicit: event-triggered campaigns (`type: transactional` + `event:`) reference the triggering event's payload as `{{event.<field>}}`; `{{trigger.*}}` is for transactional *messages*, API-triggered broadcasts, and webhook/object/relationship-triggered campaigns. The frozen `references/erp-send-webhook.md` example used `{{trigger.<field>}}` for "event-carried" params — misleading for the event-triggered flows we mostly build. Verified `{{event.*}}` resolves correctly via `previews/webhook` on campaigns 113 (maidName/cancellationPaperLink) and 117 (ccapp_link/accommodation_link/accommodation_app_link). So campaigns 113 and 117 are CORRECT as built; no fix needed. Added a namespace note to erp-send-webhook.md § Filling parameters.
- 2026-07-20 — **Auto-updates toggle confirmation — DERIVED FROM CODE, built as campaign 117; ERP send-message webhook does NOT auto-inject generic params (key finding).** Trace + zero-corrections echo-back (ask-code 40482). Two client templates `CM_ENABLE_AUTO_UPDATES` / `CM_DISABLE_AUTO_UPDATES`, sole send sites `ContractService.enableAutoUpdates`/`disableAutoUpdates` (module magnamedia-client-management), fired on the contract `enableAutoUpdates` toggle (ops UI POST enable/disable-auto-updates/{id} + ChatGptController.toggleAutoUpdates), suppressible via transient `skipMessage`, recipient = CLIENT, owner = contract, NO persisted sent-flag. Built (env 216662, DRAFT, owner Hassan): one campaign 117, event `auto_updates_toggled` keyed client, no filter, rematch, **branch on `{{event.enabled}}`** → CM_ENABLE (768) / CM_DISABLE (769) → exit; render-tested both branches PASS. **KEY FINDING (ask-code 40482 Q3):** unlike the entity-resolved send used by mv_maid_under_24, the ERP `clientmgmt/template-message/send-message` webhook does **NOT** replicate the internal `MessagesUtils.sendMessageForClient` generic-param merge (`ccapp_link`, `accommodation_link`, `accommodation_app_link`, conditional `cancellation_settlement_link` via VoiceResolverSMSHelper) — it passes `parameters` through untouched. So for templates that rely on those generic link params, **CIO must carry them in the event and pass them explicitly** (empty `parameters` → blank body). Design: the ERP event carries the pre-computed generic superset; CIO maps `@param@ → {{event.<param>}}`. Reusable rule: before assuming empty-params/entity-resolution works (as for mv_maid_under_24), confirm whether the specific send path is entity-resolved (SpEL, server-side) or generic-param-merged (internal-only) — the webhook only does the former. OPEN: U1 exact per-template placeholder list (DB/LivePerson, needs mmdb) — publish-blocker; U3 Snowflake volume; U4 ops-endpoint duplicate sends.
- 2026-07-18 — **Naming convention (Hassan): campaigns developed in THIS environment carry `(Hassan)` as the owner, regardless of who the source sheet/Jira lists.** Standing instruction from Hassan. The `(<Owner>)` suffix in the campaign naming standard (`CIO - <MV|CC> - <Clients|Housemaids> - Cluster <N> - <Journey> (<Owner>)`) = the person developing it here = Hassan. Applied retroactively: CIO campaign **114** renamed `... Maid Under 24 (Karim)` → `... Maid Under 24 (Hassan)` (name + description, env 216662) and its build spec/board updated. (The mv_maid_under_24 source sheet listed Karim; that's the requester, not the CIO owner.)
- 2026-07-18 — **`mv_maid_under_24_message_template` ("Maid Under 24") — flow DERIVED FROM CODE (no System-1 board), zero-corrections (ask-code 40450); ERP age-boundary discrepancy flagged; CIO design = source-gated event.** Board-less derivation via `references/derive-from-code.md` (ad-hoc request, Karim). Module `magnamedia-prospects`; sole send site `HousemaidController.sendMvMaidsUnder24Message` L383–388; **recipient = CLIENT** (`contract.getClient()`, `SmsReceiverType.Client` — the maid is only the subject of the copy), owner = contract. Three trigger sites, two types: hooks `ContractController.createContractPhaseTwo` L847 + `processMaidVisaPostponedContract` L3032 (via `checkAndSendMvUnder24Message`), and event `@BusinessRule` Housemaid `AfterUpdate` on birthdate/nationality → `needsMessageMvUnder24` → ACTIVE MV contract. Eligibility: `status==ACTIVE` AND `contractProspectType==MAID_VISA` AND `nationality.code=='philippines'` (Philippines-only, despite the generic name) AND under-24; Layer-1 guards = client reachable (whatsapp OR mobile) + **once-per-contract dedup** (TEMPLATE_MESSAGES on template+contract). **DISCREPANCY (real, confirmed): hook path age `<24` STRICT (ContractController L4403) vs event path age `<=24` INCLUSIVE (RegeneratedContractOnFillMaidInfoBR L55)** — a maid exactly 24 fires via the maid-update event but NOT via the contract hooks. This is an ERP inconsistency to reconcile at the source; CIO source-gating mirrors whatever ERP does, so it needs no CIO fix. **No Java-supplied params** (`.addParameters` never called); ask-code 40451 further established all placeholders are **expression-type** DB params (SpEL against the SalesNotificationTarget entity context), **no value params** (else the empty map would throw) and no framework-wide generics → the CIO ERP-send webhook `parameters` block is **likely empty** (ERP resolves them server-side from the entity via `entityType=client`+id). Narrow residual: confirm the webhook resolves any **maid-referencing** expression params under `entityType=client` — close via mmdb template body or a Verifier fixture-render (no longer a hard build-blocker). Channel WhatsApp. **Proposed CIO design:** single source-gated event `mv_maid_under_24_message` keyed to the client (`c_<clientId>`), emitted post-dedup at the send site (inherits once-per-contract), ERP-send webhook `entityType=client`; re-entry `rematch`; NO sync-add (age/nationality/type stay source-gated, never branched in CIO), NO new relationship/deletion. `birthdate`+`nationality` are persisted/syncable but not needed under source-gating. Build spec: `work/derived-flows/mv_maid_under_24.build-spec.md`; raw trace `work/derived-flows/mv_maid_under_24.flow.md`. DERIVED FROM CODE = higher risk → recommend feeding back to System 1; blind Verifier is the primary safety net. Board deferred until params (U1) resolved.
- 2026-07-18 — **`visa_sa_fake_app` ("Prospect visa document re-upload") — OUT OF SCOPE confirmed by code (reinforces the 2026-07-02 cluster-21 manual-review call, now with a code reason).** Traced via ask-code 40449 (`erp/magnamedia-prospects`). Send site `VisaServiceApplicationController.sendFakeAppSms` (template constant SalesModule L431); two triggers — cron `FindAndCloseFakeAppsJob` (every 15 min → closeFakeVisaApps → sendVisaSms) and synchronous `POST /processapp` when `validateApp` fails (EID+IBAN rejections present, no unaddressed account-holder-name rejection). **Recipient is NOT a resolved entity: `SalesNotificationTarget` built with `ownerEntity=null` AND `receiverEntity=null`; the target is the raw applicant phone (`application.getApplicantNumber()`, normalized) in both phone+whatsapp fields, `receiverType=Prospect`.** With no Client/Housemaid entity there is nothing to key a CIO profile on → genuinely un-migratable as a Clients/Housemaids campaign, independent of the domain-scope call. Params `whatsapp_link`/`link`/`greetings`(hardcoded)/`workerType` + a `hasAccountHolderNameRejection` context flag for in-template copy variation; SMS primary (real fallback in core `TemplateUtil`, outside this module — unconfirmable). **Decision: stays OUT of MV-Clients** (keep as manual-review); a future sales/prospect-target migration would first need a prospect-profile data model. Trace: `work/derived-flows/visa_sa_fake_app.flow.md`.
- 2026-07-16 — **Cluster-8 (EID-Biometrics) — the "sync not live" diagnosis was WRONG; real blocker is a `latest_visa_step` CASE-PRECEDENCE bug (dual-task shadowing).** Checked all 19 currently-eligible at-step maids (ACTIVE contract + client, mmdb) against live CIO. The sync IS deployed and carrying `pending_eid_biometrics` — but only **8/19** resolve to it; the other **11/19** resolve to `waiting_for_medical_test_eid_fingerprinting` and land in campaign **125**, not 128. Root cause: these 11 maids have BOTH workflow tasks active in a single comma-joined `TASK_NAME` (e.g. `'Pending maid to go for EID Biometrics,Waiting for the maid to go to medical test and EID fingerprinting'`), and the sync's `latest_visa_step` CASE (Contracts Groups Query L555-560) tests the medical-step LIKE **before** the biometrics LIKE → medical wins. The 2026-07-16 reorder (that put medical first to fix R-Visa/work-permit shadowing) thus created a NEW shadowing: `waiting_for_medical` masks `pending_eid_biometrics`. Since a single-valued `latest_visa_step` cannot represent a maid at two steps and ERP fires BOTH senders (each gates on its own `isAtStep`), the dual-task maids MISS 128's biometrics messages. **ERP send-logic resolved (ask-code 40217):** the two message SETS are NOT mutually exclusive by design — each gates on its own step's `isAtStep`/`isCompleted` independently — BUT `MED_BIO_SAME_DAY=true` (prod) suppresses the medical-test chain 8_1–8_5 for New/first-EID maids (`MaidVisaJ2MessageService:96`: skip when `eidApplicationType != 'Renew'` AND `med_bio_same_day==true`). So a both-step maid gets: **New → EID-bio ONLY** (medical reminders suppressed); **Renew → BOTH**. **All 11 concurrent maids are `eid_application_type=New`** (mmdb), and there are 0 Renew maids at the biometrics step → ERP sends them biometrics-only (Snowflake confirms: 9 had live 38624/22498 sends, no medical reminders). So **128 is the SOLE correct owner**; routing them to 125 is wrong AND 125's own `eid='Renew'` front-chain gate (Cluster-6 Fix D) would block them anyway → they currently get NOTHING in CIO = real miss. **Recommended fix (current population): reorder the sync CASE so `pending_eid_biometrics` is tested BEFORE `waiting_for_medical`** — moves all 11 New concurrent maids into 128; 125 doesn't want them. Fully-robust fix (only needed if a Renew maid can ever be at both steps — 0 today): decouple 128/125 from single-valued `latest_visa_step` onto independent task-presence booleans (`at_pending_eid_biometrics`, `at_medical_test_eid`), matching ERP's independent gates. **EMPIRICALLY CONFIRMED (Snowflake, 60-day actual sends to the 11 concurrent clients):** every one received ONLY bio-flow templates — `MAID_PENDING_MEDICAL_BIO` (38624) and `PENDING_EID_BIOMETRIC_REMINDER` (22498), many today — and **ZERO** medical-test reminders (no 8_1/8_2/8_3/8_5/8_6/8_24). This both (a) proves 128 is the correct owner (not code inference — real sends) and (b) confirms `MED_BIO_SAME_DAY=true` live (the absence of 8_x to these New maids only happens under true) → the Setup-admin check is no longer needed. Note on intuition (Moe asked "shouldn't both-step maids get medical reminders not bio?"): they DO get a medical-aware reminder — `MAID_PENDING_MEDICAL_BIO` = "pending MEDICAL + bio" is the bio-flow's consolidated reminder; MED_BIO_SAME_DAY intentionally replaces the standalone 8_x chain with this single track to avoid double-messaging. Fix (CASE reorder biometrics-before-medical) confirmed correct. Of the 8 in-population: 3 actively drafting (clients 374567 / 470248 / 436264, all `MAID_PASSED_PENDING_MEDICAL_BIO`); 5 in-population but silent (449588 / 315083 / 471979 / 483093 / 503624) — all `pending_eid_biometrics` + medical completed + not-present + ongoing, no draft because they have no client-specified medical date → node 5213 (`client_specified_medical_date EXISTS`) short-circuits to no-send (the appointment-date data gap final-check §9 flagged; ERP's own job also `is not null`-gates, so likely faithful). Supersedes the earlier "deploy + backfill" framing — deployment is not the issue; CASE precedence + the dual-step modeling is.
- 2026-07-16 — **Cluster-8 (EID-Biometrics) go-live gates (final-check Rev-3): Al Ain waived; latest_visa_step sync not-yet-live is the sole remaining blocker.** (a) **Al Ain suppression — ACCEPTED DELTA (Moe: "I don't care about al ain").** Campaign 128 has no `clientCity == "al ain"` skip gate, so Al Ain clients fall into the non-AD split and get 38624/38625/22498 where ERP sends nothing. Population ≈ nil → do NOT re-flag as a defect. (b) **`latest_visa_step` reorder is correct in the query SOURCE but NOT yet live/backfilled.** Verified `~/Desktop/Clients & Housemaids Query/New CIO Queries/CIO Contracts Groups Query.txt` L551-575: the `pending_eid_biometrics` branch (LIKE '%Pending maid to go for EID Biometrics%') is present and correctly ordered ABOVE the R-Visa/work-permit flag branches (fixes the shadowing). BUT mmdb ground truth = **24 NewRequests active at the biometrics step right now**, while campaign 128 drafted for only ~1 person (3 drafts) → the corrected sync query is not deployed to the live CIO sync / not backfilled, so ~23/24 in-step maids are currently uncovered. Go-live gate: deploy the query to the live sync + backfill the attribute, then re-run final-check. (0 RenewRequests at this step — new-request-only, consistent with design.)
- 2026-07-16 — **Cluster-8 (EID-Biometrics) cio-design CORRECTED — DESIGN wrong, build (CIO campaign 128) right.** Go-live final-check Rev-3 adjudicated the built campaign against ERP via ask-code sessions **40210 / 40211** (erp/magnamedia-visa-processing) and found two design mis-statements, now fixed in `work/MV-Clients/Cluster-8-EID-Biometrics/cio-design.md`. **(1) §4 A12/A13 — 22498 (PENDING_EID_BIOMETRIC_REMINDER) is NOT a blanket future-date reminder.** It requires the medical step NOT completed (`!isCompleted(WaitingMedicalTestAndEIDFingerprintingStep)`), so future+medical-completed → 22498 does NOT fire: future+completed+non-Dubai → client gets NOTHING; future+completed+Dubai → gets only 24785. Added a medical-not-completed branch (new A13 gate → A13a send). (ask-code 40210) **(2) §7 same-day double-send — the design's claim that 24785 co-fires with Path-1/2 on future OR past dates is WRONG.** 24785 (PENDING_EID_BIO_DUBAI) requires medical COMPLETED and `diff = clientSpecifiedMedicalDate − today` with `diff>=0` (a past date, `diff<0`, blocks it). The ONLY real same-day co-fire is `diff==0` + Dubai + medical-completed → 38625 (MAID_PASSED_PENDING_MEDICAL_BIO) AND 24785, together (independent dedup stores VisaTemplateMessageRepository vs VisaPushNotificationRepository, no cross-guard). Future: 24785-needs-completed vs 22498-needs-not-completed are mutually exclusive → no co-fire; past: 24785 blocked → no co-fire. (ask-code 40211) Value-gotcha checklist + §8 fidelity note 7 updated accordingly. No re-build needed — CIO campaign 128 already matched ERP; the doc was the error.
- 2026-07-16 — **Repeat-Medical Finding 1 (repeat 8_9) — Moe: FOLLOW ERP (Option A, full parity).** The validator's PASS-WITH-NOTES left one conscious business call: ERP re-fires `MAIDVISA_J2_8_9` (client abscond-warning) per unattended repeat-medical **sample date** (1st sample +5wd, 2nd/3rd/4th +2wd), no dedup — Snowflake ≈1.72 sends/recipient — but the CIO design sent 8_9 **once**. Moe chose ERP parity: model 8_9 as a **two-campaign self-loop** (same pattern as 8_8) — after each 8_9, re-check next-unattended-sample-date + `maid_present==false` + step-active, re-fire until 8_10 dispatch or she presents. Folding in the two remaining ERP-fidelity findings at the same time: **Finding 2** (E5n → `clientCity IS NOT NULL AND != 'Abu Dhabi'` so a null-city maid gets no 8_9, matching ERP's null-city guard; still gets 8_10) and **Finding 3** (pin renewal→110 (N3) to fire only for the 4-set `{ISOLATION, NEEDS_TO_REPEAT, PRESCRIPTION_AND_REVIST, PPD}`, or add an explicit `medical_result ∈ 4-set` gate). Carried-forward data-structure requirement unchanged: `maid_present_at_medical_center` must be redefined latest-row → **all-rows EXISTS** at go-live. Re-translate + rebuild combined board + re-validate before System 2.
- 2026-07-13 — **System-1 steps 1-2 (interrogate → legacy board) run for ALL 10 un-migrated MV clusters** (4 GCC, 9 Visa-progress, 10 EID-delivery, 12 MV-salary, 15 Cancellation-paperwork, 16 Renewal-signature, 17 Birthday, 19 Name-completion, 20 Auto-updates, + Repeat-Medical split from 7). All 10 flow-specs zero-corrections (started from clusters.md verified hints); all 10 legacy boards in MV "Legacy ERP Boards" folder `9ka6KvrhJC3Zf3L6Te1Ar5`. **Sibling check: NO templates need pulling in** — every not-in-export sibling is maid-target (housemaids migration), CC-domain, or dead in 2026 (verified Snowflake: MOHRE_ABSCONDING/J2_7_3/J2_7_5/2_1_6/etc. = 0 sends 2026; RESIDENCY_VISA_HAS_BEEN_ISSUED_CLIENT = 2/dead). **Clustering held** — no in-scope template re-clustered; Repeat-Medical split validated (8_7-8_11 genuinely repeat-medical). **One reclass:** Cluster-15 `get_from_client_maid_active_phone_number` (53191) is ERP-send but n8n-orchestrated → moved to manual-review (scope decision; 8812 stays). Residuals for later stages: Cluster-9 Upd08 has no MAID_VISA gate (~3% CC leak); Cluster-4 53603 near-zero delivery; Repeat-Medical reminder-chain echo-back not re-run (8_8 self-gate / 8_10 no-city-gate to confirm). ENV NOTE: a mid-batch API outage killed agents on final write/board-draw; recovered by resuming (single-write) + single-`create` boards (the reliable Whimsical pattern when `edit` is unavailable). Board URLs in each cluster's boards.md.
- 2026-07-10 — **Cluster 6 (MV Medical-Test Reminders) FINALIZED build-ready** — dual-validated GO (low-code ask-code 39770–39782 + Snowflake real-data). Five live-config-driven fixes baked in (config: `med_bio_same_day=true`, `DAYS_BEFORE_CLIENT_MEDICAL_DATE_REMINDERS=4`): **[A]** `visa_last_day` = COALESCE(COS "Upload Change of status" move-out +60d, entry-visa expiry) — NOT `LAST_DATE_ALLOWED_STAY` (which is a sparse manual field; `LAST_DAY`≈100% null); Moe applied this to the live sync. **[B]** client-date = per-send suppression window `[date−3,date]` (N=4), **AD two-sided (stops after the date), Dubai one-sided (keeps sending after)** — verified live (39754/39761/39798), overturning a stale-snapshot read; the AD *seeding* gate is also two-sided so a client date ≥4 days out at step entry → **zero AD reminders** (a genuine ERP gap, 39800). **[C]** loop = `REMINDER_4` only (AD), unbounded via CIO self-retrigger; no active Dubai loop (8_4 7-day-gate-suppressed). **[D]** Dubai `8_1–8_5` gated `eidApplicationType='Renew'` (New/null → same-day biometric flow = Cluster 8); needs SYNC-ADD `eid_application_type` (spec in golive/data-structure.md). **[E]** whole campaign gated `request_type='new'` (renewals go to the separate RENEWAL_MV_MEDICAL_REMINDER_* family; sync `latest_visa_step` COALESCEs vrr so without the gate it wrong-sends to ~half the audience). Also: inner `clientCity='dubai'` gate for 8_2/8_3/8_5 (non-Dubai emirates get 8_1+terminals only); `visa_last_day` null ≈0.1% (guard the deadline waits). Build-ready board https://whimsical.com/PhUBuBe9bo6WVFJCX6B51s (explicit waits) + cio-design.md build sheet + companion REMINDER_4-loop campaign. Build prereqs: sync-add `eid_application_type` (A already live), event `medical_r4_tick`. LESSON (folded to low-code-authority memory + code-llm-api.md): live ask-code is authority; the `~/Desktop/magnamedia` snapshot is fallback-only and lost on 3 conflicts (isWithinReminderThreshold, AD gate one-vs-two-sided, 8_24 gating).
- 2026-07-10 — **NEW cluster: Renewal-MV-Medical-Reminders (MV-Clients, ad-hoc Mode B) — System 1 COMPLETE, GO WITH NOTES (design).** The visa-RENEWAL counterpart of Cluster 6: 6 templates `RENEWAL_MV_MEDICAL_REMINDER_{DUBAI,AUH}_{INITIAL,FOLLOWUP,RECURRING}` from `RenewalMvMedicalTestReminderService` (RenewRequest; recipient=CLIENT). Interrogate (zero-corrections 39826) → diagram → attribute-map (NO new intake — sync rebuilt renewal-aware 2026-07-10, carries `days_in_medical_eid_step`) → snowflake-validate → translate → validate (ask-code up, independently re-derived). Shape: EVENT-triggered (`renewal_medical_reminder_started`, client-keyed), city fork **AUH = abu_dhabi OR alain** (adds Al Ain vs Cluster 6) / Dubai catch-all; cadence INITIAL(immediate)→FOLLOWUP(ERP day4=synced 3)→RECURRING(ERP day7=synced 6, then +3, **UNBOUNDED** — no deadline/dedup/terminal/mutation) via two-campaign self-retrigger; stop = `maid_present_at_medical_center` / step-exit. Cadence OFF-BY-ONE: synced `days_in_medical_eid_step` has no +1 vs ERP `dayInStep`. Board https://whimsical.com/5rFVFMkM7iwLTSqd4kAbuo (+ legacy F2hYPv21FT9S6aMWEHtVN8). **🔴 GO-LIVE PREREQUISITE (not a design defect, Moe elected to proceed):** ~86% of the renewal-medical audience is NOT synced to CIO (contracts long-CANCELLED, aged out of the ACTIVE/POSTPONED + 30-day windows; 1,832 at-step maids, only ~10% ACTIVE/POSTPONED, ~14% have any synced client). Neither a relationship nor an event trigger reaches them until the sync is expanded to the renewal pipeline — a data-team change above the campaign. Minor delta: ERP's stateless daily job re-admits mid-cadence eligibility; CIO's entry-event can't (rare miss). Brand-new family (live 2026-07-10, ~1 send) → send-parity deferred to System 3. NOTE: `RENEWAL_JOURNEY_4_x` (CC, maid-facing) was briefly started then aborted — different journey, out of scope.
- 2026-07-09 — Cluster 5 (MV Missing/Rejected Visa Document Collection) migrated end-to-end (all 6 System-1 stages): **GO WITH NOTES**. Design = the two-campaign self-retrigger loop (C1 relationship-attr entry + FIRST send + fires CIO-internal `visa_docs_reminder_due`; C2 event-triggered stage-wait +1d/+2d/+1d → reminder → re-fires at itself, daily forever). Boards: legacy https://whimsical.com/G77ZZujc2xXJfVwo9ZSiPc, CIO https://whimsical.com/4sTKzdV88SF5EAL28yN1e2. Intake: **1 sync-add only** (`sms_maid_visa_enabled` on Clients Profile), 0 new events/APIs/relationships — the doc computation is already synced (Contract obj `visa_documents_status`/`missing_documents`/`rejection_documents` + B3 rel). **REVERSED track mapping is CODE-TRUE and DATA-SEALED** (re-derived blind session 39686; `hasBothMissingAndRejectedDocuments` enum value → MISSING_DOCUMENTS family 53538/9/40; not-both → MISSING_OR_REJECTED family 54092/3/4 ~97%; real-data 98.2% vs 29.2% rejections at send time). Branch on the enum VALUE, never the template name — do NOT "correct" it. Maid-driven eligibility, **NO `contract.type='MV'` filter** (would drop 16 dual-contract CC-newest clients). **GATED BUILD BLOCKER for System-2 data-structure (Finding #1):** the sync's `visa_documents_status` does NOT replicate ERP's passport-rejected-for-expiry exclusion (`excludePassport`, session 39692) — a passport-expiry-only case ERP suppresses could make CIO fire (wrong audience) or flip the track to BOTH (wrong template); MUST be closed before build. Minor non-blockers: design exits permanently where ERP suppresses-then-resumes (rare, always under-send); re-entry restarts at FIRST (copy impact ~nil, shared body); JSON doc-list param rendering is a flagged build decision (liquid vs pre-render). No double-send / wrong-person in admitted audience. Snowflake-validate PASS. Cluster 5 = most-frequent pending cluster (7,533 sends/45d) now System-1 complete.
- 2026-07-02 — MV-Clients clustered: 132 = 91 clustered (21 clusters) + 25 manual-review (n8n/not-in-ERP) + 16 notifiers (excluded). Visa lifecycle (MaidVisa visa-processing engine) dominates (clusters 1–14). 0 stale; single-digit senders marked `low-volume` inline, not stale.
- 2026-07-02 — Clustering heuristic added to cluster-analyzer playbook: lowercase name + Jira-ticket Triggerer ⇒ likely n8n/external (validated 25/25 on MV-Clients).
- 2026-07-02 — Business-journey merges accepted over strict technical-trigger splits for MV-Clients clusters 2,3,7,9,10 (multiple trigger steps, one client story) and cluster 13 (payroll + housemaid-management modules, one "salary payout" journey). Each flagged to split later if the CIO design gets crowded.
- 2026-07-02 — RESOLVED (Moe): cluster 21 `visa_sa_fake_app` → manual-review (out of MV-Clients scope; parked for future sales-target migration). Cluster 18 OTP → manual-review (transactional, out of campaign-migration scope). New MV-Clients tally: 89 clustered / 19 clusters / 43 manual-review. Precedent: transactional (OTP) and prospect/sales-target messages are out of scope for target migrations.
- 2026-07-02 — Pilot cluster chosen: Cluster 14 (MV contract termination/cancellation) — first end-to-end run through System 1.
- 2026-07-03 — Infra fixes to scripts/ask-code.sh after two agent stalls: poll wait 180s→600s (ASK_CODE_TIMEOUT overridable), per-request curl --max-time (120s submit / 60s poll), poll page size 20→8 (large pages hang the endpoint). Key operational insight (also in code-llm-api.md): a timed-out conversation KEEPS processing server-side — re-poll the same conversation_id, never re-ask. Recovery pattern that worked: harvest ready answers to disk (work/<...>/raw-answers/<id>.md), then run the interrogator in SYNTHESIS mode reading from files instead of gulping the API.
- 2026-07-03 — BUILT: new System 1 stage `attribute-mapper`, inserted between flow-diagrammer and customerio-translator (/migrate-cluster stages renumbered 1-5). For each attribute/condition/param the flow needs, it uses ask-the-code to find the DB source (table.column) + value semantics, checks if it's already synced to CIO, and classifies the CIO intake: synced attribute / sync-query addition / API-call journey attribute / event (persisted-vs-transient gate). Output work/<t>/<c>/attribute-map.md, consumed by the translator (which now annotates each attribute's source on the CIO board) and feeds the go-live api-spec-writer / data-structure-designer. Wired into migrate-cluster.md, customerio-translator.md, CLAUDE.md. NOTE: pilots 13/14/1 predate this stage (their translators did inline gap analysis) — not retrofitted unless re-run.
- 2026-07-03 — Cluster 2 scope (Moe): pull in EVERYTHING live-in-code, marking not-in-export items as such. Includes the Stage-2 lane initials + REMINDER_2 (J2_2_2, ACTIVE_VISA_WITH_PRIVATE_SPONSOR) AND the full Tadbeer 7PM old-visa-cancellation engine (OLD_VISA_CANCELLATION_1_1/1_2/1_5/1_6 + the every-3-days 7PM 3674 re-send). Cluster 2 = the complete prior-employer-block + old-visa-cancellation journey. The 3674 dual-sender overlap (workflow-step lane vs 7PM engine) must be reconciled in the flow-spec before diagramming. General precedent reaffirmed: pull in all live in-code siblings, mark not-in-export.
- 2026-07-04 — Replacement-Handover pass 3 (Moe): DROP the fromCCApp check entirely. Removed the `fromCCApp?` branch in Campaign 1; confirmation sends now go to ALL admitted replacement creations regardless of origin. ACCEPTED DEVIATION from ERP (ERP confirms only CC-app-created replacements via POST /appcc/replacement/create; ops-created ~98.5k did not get a confirmation — now they will). Per Moe "don't care about the CC-app distinction." Board + cio-design.md updated (pass-3 superseding note + build-sheet step 3 removed/step 4 & converge ungated). NOTE for future validation: do NOT flag "confirmation over-sends to ops-created" as a bug — it's this accepted deviation. Phase B unchanged.
- 2026-07-04 — Replacement-Handover FINAL: **GO** (consolidated). Fix A re-validated clean — ops-created missed-pickup now gets tmpl 5/6/7; ops-created still gets no CC-app confirmation; no leak/double-send; all prior fixes + separate campaigns intact. Final structure: 5 campaigns (Campaign 1 "Replacement Lifecycle" = former C1+C2 merged with a post-entry fromCCApp branch gating only the confirmation; C3 found-in-attendance, C4 new-maid-returned, C5 overlap ladder, C6 refund kept separate for stated CIO reasons). Cluster COMPLETE end-to-end. Residual go-live notes R1 (E4 complaint sub-path null-guard) + R2 carried, non-blocking.
- 2026-07-04 — Replacement-Handover consolidation re-validation: FAIL → repair (fix A, Moe). The C1+C2 merge put Phase B (missed-pickup tmpl 5/6/7) behind Campaign 1's `fromCCApp=true` entry filter, but ERP sends those to ALL live-in replacements regardless of origin (~98.5k ops-created) → missed send. Fix A: remove fromCCApp from Campaign-1 ENTRY (admits all E1 creations — consistent with the endorsed E1-widening); gate ONLY the confirmation sends with a `fromCCApp?` branch; Phase B runs for all. Keeps the merge. Latent bug (original GO also missed it — only traced ops-created through confirmation, not the missed-pickup path). Merge/split otherwise judged maximal-and-correct by the validator.
- 2026-07-04 — Convention (Moe): prefer ONE campaign per journey; the first Replacement-Handover design over-split into 6. Consolidate C1–C5 (all flow sequentially from replacement_created) into one "replacement lifecycle" campaign via waits/wait-untils/branches; keep C6 (refund) separate (orthogonal). Split only when CIO forces it (concurrent independent event-waits, different cardinality, orthogonal outcome, unbounded loop). Folded into customerio-conventions.md ("Prefer ONE campaign per journey"). Re-translating Replacement-Handover to consolidate + re-validate.
- 2026-07-04 — Cluster 3 scope (Moe): "complete the exported chains only." IN: PAY_OVERSTAY_FINE_OUTSIDE_DUBAI (initial+R1+R2, exported), PAY_OVERSTAY_FINE_INSIDE_DUBAI (one-shot, not-in-export, pulled in), REJECT_REDUCE_FINE initial+R1 (not-in-export, pulled in) + R2 (exported). OUT/deferred: the APPROVE_REDUCE_FINE (+R1+R2) branch (status=Approved). All 4 exported + pulled-in siblings go to the CLIENT (verified). Interrogation echo-back clean; duplicate-send risk (no sent-flag) + 12181 amount-source divergence (displayed OVERSTAY_FINES_AMOUNT vs payment-link getOverstayFine()) noted.
- 2026-07-04 — Cluster 2 FINAL: **GO** (re-validated, session 39368). 1_5 fixed to ordinary client send (board B14 verified via MCP); API1 null→work_permit fixed (ERP guard is negated-equals `!Company_Sponsorship.equals(...)` → null + non-Company → J2_2_5); endorsed deviations (1_3 cross-suppression, private-sponsor proof-exit) intact; no regression. Cosmetic attribute-map blocker_type sample `other` added. **Cluster 2 COMPLETE — first cluster through the full SIX-stage System 1 (incl. snowflake-validate).** 4 CIO campaigns (A-Main/A-Loop clearance, B-Main/B-Loop Tadbeer). Board 2tphEAc4AaggMVvYZbvb5Q.
- 2026-07-04 — Cluster 2 validation: FAIL → repair. BLOCKER: `OLD_VISA_CANCELLATION_1_5` is delivered to the CLIENT (client's own phone), NOT to Tadbeer — the copy is *addressed to* Tadbeer but `send1_5Message` resolves recipient=client (fresh sessions 39364/39365). This overturns Moe's decision-2 premise + the flow-spec §2A/§D/§5 claim. FIX: model B14 as an ordinary CLIENT send (drop the webhook / Tadbeer-profile framing — simpler); correct flow-spec + attribute-map + cio-design. Second fix: API1 must map active-work-permit + null/any-non-Company prev-visa → work_permit lane (never `other`), else null routes to Exit and misses J2_2_5. Deviations that HELD: cross-suppression on 1_3 (endorsed), private-sponsor proof-exit restoration (endorsed). LESSON folded into code-interrogator.md: recipient ≠ addressee — trace the actual send target in code, never infer the recipient from who the copy talks to.
- 2026-07-04 — Cluster 2 translate decisions (Moe): (1) OLD_VISA_CANCELLATION_1_3 dual-sender → TWO separate campaigns + cross-suppression (blocker-clearance lane vs Tadbeer old-visa-cancellation workflow; they only share the template). (2) OLD_VISA_CANCELLATION_1_5 → INCLUDE (sent TO Tadbeer, a partner — NOT a client/maid). Wrinkle: 1_5 breaks the person-target model; translator must specify CIO delivery to Tadbeer (static Tadbeer contact/profile or outbound API/webhook) and flag as a go-live item. Structure target: consolidate the 5 mutually-exclusive blocker lanes into ONE "prior-employer clearance" campaign (branch on blocker-type discriminator; shared reminder loop; stop-on-proof-tag), keep Tadbeer workflow separate. Reminder loops are unbounded repeat-until-proof → two-campaign loop pattern. Restore the commented-out ACTIVE_VISA (private-sponsor) proof-exit or flag.
- 2026-07-04 — Cluster 2 attribute-map: 31 attrs → 5 synced, 2 new APIs (`getPreviousEmployerBlock`, `getVisaClearanceLinks`), ~7 events (prev_employer_block_detected, proof-uploaded ×3, old-visa-cancellation started/client-took-action/tadbeer-confirmed), 1 optional sync-add (maid gender), 6 dropped artifacts. Discriminator NOT synced → API. Landmine: `typeOfPreviousVisa` is an INTEGER ORDINAL (Company_Sponsorship=3/Private=4/Tourist=1) — string branch never matches; folded into customerio-conventions.md ("verify VALUES"). Translator must add the OLD_VISA_1_3 dedup + restore the commented-out private-sponsor proof exit.
- 2026-07-04 — Replacement-Handover RE-VALIDATION: **GO** (was FAIL). Both blockers fixed (C2 catch-all else via E2 3-value bucket closed/pending/other; C3 null case via event-at-send-point, no CIO taxi gate), confirmed fresh sessions 39332-34, no regression. Cluster complete end-to-end (interrogate→diagram→attribute-map→translate→validate). Residual non-blocking: R1 — E4 complaint sub-path has a null-guard where ERP skips send but mutation/E4 still runs (add caveat to the dev-validation task; vanishingly rare); R2 — flow-spec Lane-2 wording sharpened to state the catch-all + null-case explicitly (done). Snowflake-validate not yet run for this cluster (postdates it).
- 2026-07-04 — Replacement-Handover validation: FAIL → repair. Two translation-narrowing misses (flow-spec was accurate): C2 taxi-status split omitted the catch-all `else` (DONE/CANCELLED → no send); C3 required non-null work order but the ERP found-in-attendance site also sends on null (the two collapsed E3 sites disagree on null). Both deltas + both fidelity improvements otherwise survived. Fresh sessions 39326-30; mmdb confirmed all 6 sync-add columns. Lessons folded into customerio-conventions.md ("Preserve catch-all branches; verify before collapsing send sites"). Looped back to translator for one repair pass.
- 2026-07-04 — Session transferability: no pasted orchestrator prompt needed — CLAUDE.md is the auto-loaded standing brief and all state is on disk. Closed two cold-start entry-point gaps with durable commands: added `/cluster <target>` (Mode A: ingest export → cluster-analyzer → clusters.md) and extended `/migrate-cluster` with `--templates A,B,C` (Mode B: migrate a known ad-hoc cluster with no prior export). "Entry points" section added to CLAUDE.md.
- 2026-07-04 — BUILT: `snowflake-validator` — new System 1 stage AFTER attribute-mapper, BEFORE translator. Empirically validates the drawn theory against real send data (Snowflake `BA_VIEWS`). Precision-only (did real recipients meet the drawn conditions); 7-day window widening to 14 if <~20-30 recipients; hard/systematic mismatch → loop back to code-interrogator, else PASS WITH DATA NOTES to Moe. Verified access + schema in docs/snowflake.md; `scripts/sf_query.py` (key-pair, reads .env). Wired into /migrate-cluster + CLAUDE.md. Snowflake key pasted in chat → ROTATE.
- 2026-07-04 — Snowflake join gotcha (verified): recipient→client join is `BROADCASTS_FINAL_LAYER.RECEIVER_MOBILE_NUMBER = CLIENTS_LIVE.NORMALIZED_MOBILE_NUMBER` (~98%). The `NORMALIZED_*` columns ARE the SHA-256 hash; the `HASHED_NORMALIZED_*` columns are EMPTY decoys; `SHA2()` of plaintext does NOT reproduce the hash. History via `*_STATUS_LOGS`/`*_HISTORY`/`*_REVISION` for point-in-time. Documented in docs/snowflake.md.
- 2026-07-03 — Cluster 1 (Postponed MV nudge chain) migrated: interrogate→diagram→translate→validate = **GO WITH NOTES**. Design = single sequential nudge campaign (bounded, NOT the loop pattern) + separate terminal-success campaign; dropped CSV re-send guard / cron / mv_acceptance_max_days as ERP artifacts. Board in folder KaSjst3hrHd5e9Yhou8RGm. Go-live opens for Moe: (a) `@link@` per-contract deep-link intake — contract UUID not in sync → add as contract attribute or API journey attribute (hard build dep); (b) success-pair trigger should be "became ACTIVE for MV client" not the POSTPONED→ACTIVE transition (sub-sync-cycle proceed edge); (c) titled name `@client_first_name_with_title@` (interim {{customer.first_name}}); (d) confirm DB week→template binding. Nudge week offsets (7/14/28/42/56/70/84d) verified exact.
- 2026-07-03 — Reusable pitfall (from Cluster 1): state-TRANSITION triggers miss records that jump states faster than the sync cycle → prefer destination-state trigger or event. Folded into customerio-conventions.md.
- 2026-07-03 — CC Cancellation flow validated: GO WITH NOTES. `_1/_2/_3` byte-identical (consolidation faithful). OPEN (Moe): task text says `_4/_5→_1` fallback but the board keeps `received→_4`/`PDC→_5` branching — opposite designs; the fallback LOSES the distinct refund wording in `_4`/`_5`. Recommend keeping the board's branching (preserves refund; API 1/2 support it). Minor: board's else→`_1` over-sends to a no-payment/no-DD residual ERP sends nothing to.
- 2026-07-03 — API-spec principle (from cancellation API 3): an encapsulating API can only return PERSISTED state. `isFromBouncingFlow` is a transient `@RequestParam` (never persisted) → no contractId-keyed API can return the real flag; capture it as a CIO event at scheduling time. Folded into customerio-conventions.md (in-campaign toolkit). API 1 (received) & API 2 (PDC/DD accepted) fully grounded and paste-ready in work/CC-Clients/Cancellation/api-specs.md.
- 2026-07-03 — BUILT: System 2 `golive-api-spec-writer` agent + `/prepare-golive <t> <c> api-spec` stage. Writes grounded, paste-ready API-creation prompts for Moe's API-creating ask-the-code; mandatory persisted-vs-transient feasibility gate (transient → recommend CIO event, not API). Added to CLAUDE.md System 2 diagram.
- 2026-07-03 — Cancellation _4/_5 decision (Moe): KEEP branching (board version) — received→_4, accepted-PDC/DD→_5, else→_1 — to preserve the distinct refund wording in _4/_5. API 1 & 2 (api-specs.md) support this and are paste-ready; API 3 must be a CIO event (transient flag), not an API.
- 2026-07-03 — Validator/checker false-positive corrected (Moe caught it): the CC-Sickness final-check Finding #5 wrongly claimed `..._HAS_NO_TEMP_MAID_NOTIF` "double-messages temp-maid clients." Re-verified (session 39182): the ERP job sends it ONCE (flag `clientNotifiedToComeForReplacement`), 24h after WO CLOSED, only if no maid on contract — Moe's design matches ERP and his branch already EXITS temp-maid clients, so no double-send. Finding retracted; net blockers now #1/#2/#3/#6. Lesson folded into validator.md + golive-final-checker.md: trace the campaign's own exits before asserting duplicate/double-send/wrong-audience, and verify ERP "reminder" cadence (once vs recurring) via the sent-flag before flagging.
- 2026-07-03 — System 2 stage-D (golive-final-checker) first real run: validated Moe's already-built CC Maid Sickness campaign from a pasted description (no design doc) → **NO-GO**, 5 blockers. Confirmed the checker works with a pasted campaign + self-established ERP truth (sessions 39165-67). Key blocker: wait-until `doctor_work_order_status='closed'` never fires — synced value is uppercase `CLOSED`. Reusable lesson folded into customerio-conventions.md ("Verify attribute VALUES, not just existence"): casing/format, boolean polarity (`maid_refused_to_join_client` is inverse of want_to_join), "field that isn't a field" (temp maid = `maid_role=temporary_replacement`), signals needing wiring (taxi deliver_to_client needs TAXI event). Also: at-doctor template chosen by contract-has-maid, not refusal flag; `..._WHO_HAS_TEMP_MAID_NOTIF` must be covered; sick-leave-end path decided once up front (`applySickLeaveEnded`), not raced.
- 2026-07-02 — Cluster 13 validation: PASS WITH NOTES (both campaigns); split confirmed correct. MAJOR (pre-go-live): template 1749 has NO CC/MV filter in code → Campaign A must add a `contract.type = MV` entry filter or use per-target event names, else CC and MV campaigns overlap on the shared `maid_salary_transferred` event. Generalized into customerio-conventions.md ("Scope every campaign to its target"). Minor/accepted: CIO is MORE deterministic than ERP under multi-active-contract (ERP recipient vs opt-in flag come from independent unordered queries); `statement_available` decision node's No-branch is unreachable (keep for readability or drop); events carry `notify_salary_release`/Ansari fields rather than widening the sync.
- 2026-07-02 — Cluster 14 RE-VALIDATION: **GO** (was FAIL). Boundary fix confirmed against code's inclusive edge (`checkIfDateBeforeOrEqualDaysFromMonthEnd` uses `.equals || .before`), both minors fixed (two accounting tags named; STATUS=ACTIVE excludes POSTPONED), target-scoping convention PASS (contract.type=MV), regression clean. Fresh sessions 39162-39164. First cluster fully validated end-to-end through System 1. Remaining go-live decisions (not findings): dynamic vs "next 4 days" copy; live DAYS_TO_END_OF_MONTH (the `>=` encoding is correct for any N).
- 2026-07-02 — Cluster 14 validation: FAIL (validator caught a real month-boundary off-by-one that would send the wrong template + miss a message one day/month). Fix: encode the month-timing branch as `days_until_end_of_month >= N → earlier` (N = DAYS_TO_END_OF_MONTH, default 4), definition `daysInMonth − dayOfMonth`, evaluated at entry. Also: eligibility API attr must implement BOTH accounting tags (`ignore_sending_6.5.1_sms` suppress + `from_accounting_flow` MaidVisa gate); node-12/entry status check must exclude POSTPONED (4588 job is ACTIVE-only). Looped back to translator for one repair pass. New general rule folded into customerio-conventions.md ("Encoding threshold/boundary conditions").
- 2026-07-02 — Whimsical spacing (Moe): boards were opening with nodes on top of each other. New MANDATORY rule (whimsical-standards.md + both drawing agents): space nodes generously, keep side-panels clear of the flow bbox, and FETCH the render to verify no overlaps before reporting a board done.
- 2026-07-02 — Whimsical folder discipline (Moe): pipeline boards go in a dedicated folder, separate from Moe's old MV drafts ("Customer IO - MV Clients" = drafts, disregard). MV-Clients pipeline folder = `KaSjst3hrHd5e9Yhou8RGm` ("CIO Pipeline — MV Clients"). Agents must NOT create folders / index pages / extra boards; orchestrator passes the folder ID. Fixed in whimsical-standards.md + flow-diagrammer.md + customerio-translator.md. Two pilot boards (Cluster 14 termination, Cluster 13 payroll) predate this and sit in the wrong folders — MCP has no move op, so Moe to drag them into the new folder manually.
- 2026-07-02 — SYSTEM FIX (code-interrogator): added a MANDATORY branch-completeness sweep — for every send site enumerate ALL templates the method/switch/if-else can emit; for every excluded-type or untraversed branch, ask what the other side sends and whether it's in the export; echo-back must confirm "these are ALL templates"; required new output section "Sibling & branch-completeness map". Reason: the interrogator noticed but didn't chase the payroll on-hold branch. Now routine.
- 2026-07-02 — Coverage policy (Moe): per-cluster interrogator sweep is sufficient; NO target-level code-vs-export reconciliation step. `Pay_MV_Sal_notify` confirmed NOT live by Moe → dropped, do not migrate. Refinement: the sweep must check sibling LIVENESS (actively sent, not commented-out, not target-excluded) before flagging a scope decision — only customer-facing + in-code-not-in-export + LIVE siblings get raised.
- 2026-07-02 — FINDING (export under-covers code): `Pay_MV_Sal_notify` (via `MessagingService.notifyMvClientAboutMaidSalary`) is the dedicated MV-maid salary notification and is NOT in the MV-Clients export at all (but per Moe it is not live → dropped). `Payroll_Maid_Salary_On_Hold_Notification` (maid NO_SHOW/ON_VACATION) also not in export — but it explicitly EXCLUDES MAID_VISA maids (`getOnHoldMaids` filters `housemaidType <> MAID_VISA`, HousemaidPayrollPaymentServiceV2 L2338), so correctly out of MV scope. Implication: the JourneyAI export is not a complete list of a target's live messages; the interrogator's sweep is the safety net.
- 2026-07-02 — Cluster 13 (payroll, 2nd pilot) interrogated: SPLIT into two CIO campaigns — 1749 payroll payout confirmation (event: accountant closes WPS/local-transfer todo → `MessagingService.notifyClientAboutMaidSalary`) vs 45526 Ansari statement delivery (event: ops completes GET_ANSARI_SALARY_STATMENT → `MaidServicesService.sendClientNotificationForAnsariSalaryStatement`). Divergent trigger/audience/payload/channel. Both real-time, no cron. 1749 is unguarded (re-closing a todo re-sends); 45526 dedup'd by PENDING→COMPLETED.
- 2026-07-02 — 43832 CLIENT_I_WITHOUT_EID stays in Cluster 10 (triggered by `Housemaid.cardStatus` BeforeUpdate rule `MVClientCardCollectionBR` → `HousemaidStatusChangeLogicService.createBGTForATMReadyMessage`; no shared path with payroll/Ansari).
- 2026-07-02 — Code fact: MAID_VISA maids DO receive `Payroll_Maid_Salary_Transferred_Notification` (1749) — the MAID_VISA exclusion is only on the on-hold sibling path. Folded into glossary.md (WPS).
- 2026-07-02 — Flow-spec format fix (from Cluster 14 diagramming): code-interrogator must pin the canonical message body per template + flag cross-channel discrepancies (4588's WhatsApp WABA hardcodes "next 4 days" while SMS uses `@termination_date_next_day@`). Folded into code-interrogator.md. Also added "drawing hard cases" conventions to whimsical-standards.md (additive+suppression, deferred flag hand-off, configurable thresholds).
- 2026-07-02 — Cluster 14 scope (Moe): migrate all 7 branches of the termination decision method, but mark the 4 not in the MV-Clients export as "(not in export)" on the Whimsical. General rule → whimsical-standards.md (subset-of-a-decision-tree clusters).
- 2026-07-02 — Translation principle (Moe, from Cluster 14): drop ERP execution artifacts (same-day dedup like `isScheduledTerminationSMSAlreadySent`, job-rerun guards) — don't replicate them in CIO; the state/event trigger won't re-fire. Keep only genuine business eligibility. Suppression guards → entry API call or attribute check. Folded into customerio-conventions.md ("Translate, don't mimic").
- 2026-07-02 — Cluster 14 code truth (validated, 2 echo-back rounds, zero corrections): `beforeLastDaysOfMonthCondition == true` = EARLIER part of month; `false` only in the final `DAYS_TO_END_OF_MONTH` days (code default 4, DB-overridable). The xlsx `Triggerer` wording is INVERTED ("within the last N days") — code is truth, never trust xlsx date-window wording. Also: template 4588's controller send is commented out (CM-5546); it's sent by a deferred flag + daily job `CollectionScheduledTermintationDateIsTodayMaidVisa`. Lesson: "template referenced in a method" ≠ "sent from that method" — the interrogator must confirm the send is live, not commented.
- 2026-07-02 — System 2 (go-live prep) added to the same repo: 4 gated stages (dev-task / data-structure / data-team-task / final-check), manual validation between each. Shares the base. Not live monitoring (= future System 3). Tasks are markdown → Jira by hand.
- 2026-07-02 — CIO has no export; agents read a built campaign via its Whimsical board (MCP) or Moe's pasted description. No CIO API access assumed.
- 2026-07-02 — Event design rules captured in docs/event-design.md: simple, recipient-keyed, minimal payload; propose the truest signal; use in-campaign conditions instead of events where possible.
- 2026-07-02 — Every new CIO relationship must ship with a deletion rule (CIO won't drop un-retrieved links).
- 2026-07-02 — Whimsical standards: diamonds for decisions, purple message notes, `Send <NAME>` labels, keep status overlays — provisional (Moe: "sure, I'll change it later if I don't like it").
- 2026-07-02 — Sync cadence treated as "a few minutes" (Moe says 2 min, architecture doc says 5) — never design for sub-minute freshness.
- 2026-07-02 — Segment-based campaign triggers banned: they lose contract/maid context and their attributes can't be used in-campaign.
- 2026-07-02 — Repeat-until flows use the two-campaign self-retriggering loop pattern (CIO has no loops), labeled as one logical unit.
- 2026-07-02 — n8n-sent and code-not-found templates dismissed to manual review; notifiers excluded everywhere.
- 2026-07-02 — Export `Triggers (JSON)` column rated ~50% accurate: hint for clustering hypotheses, never evidence.
- 2026-07-02 — Interrogation model: `claude-opus-4-8-high`, ≤3 parallel sessions, echo-back loop until zero corrections.
- 2026-07-07 — Medical-finished (Cluster-7): dropped the `contract.type='MV'` entry filter. Moe saw "CC" clients receiving the renewal message. Code (session 39449) + Snowflake (all 7 templates) showed ERP has NO contract-type gate — eligibility is the maid being `HousemaidType.MAID_VISA` with an active visa request; the send targets the maid's newest ACTIVE/PLANNED_RENEWAL contract of EITHER type (`ContractRepository.findTop1…`). The "CC" sightings are the ~13-14% dual-contract `Both` cohort (client holds CC + MaidVisa; for the newest-is-CC subset ERP sends against the CC contract) plus a `blank` cohort; zero pure-CC recipients. A `contract.type='MV'` filter would have dropped Both+blank — a fidelity bug. Fix: entry filter = `type='client'` only; the source-gated event is the scope. Supersedes validator finding #1 (the filter was NOT the right instrument for the AbuDhabi `isforMaidVisa` gap). Convention "Scope every campaign to its target" amended with a CAUTION: scope to the REAL eligibility (maid attr / target-specific event), add `contract.type` only for genuinely-shared templates that would double-send (1749); never add a gate ERP's send path lacks.
- 2026-07-07 — Cluster-6 (Medical-Test-Reminders): Dubai MAIDVISA_J2_8_4 is SOURCE-SUPPRESSED, effectively never sends → NOT built as a live CIO send. Validator FAILed the first design (8_4 modeled as a step-anchored +3 loop copied from Abu-Dhabi). Fresh code (sessions 39477/39480): 8_4 = inline day+7 send + deadline-anchored series at getVisaLastDay−{21,18,15,12,9,6,3}, ALL routed through the shared `case 8_1/8_2/8_4` branch gated by `remainingDays>=2` (⇒ daysSinceStepEnter<=5). day+7 → remainingDays≈0; deadline series fires weeks later → deeply negative → every occurrence suppressed (+ presentedInMedical + EID-type gates). Snowflake: 483 all-time, 0/180d. `sendWaitingMedicalTestAndEIDMessage` is a pass-through relay (not a dispatcher), so it doesn't rescue 8_4; 8_24 uses the same relay but a DIFFERENT un-suppressed switch branch (execute, no remainingDays gate) → 8_24 fires. Reverses the earlier scope call to pull 8_4 in as live (that rationale — "fills the Dubai mid-window gap" — was based on the wrong flow-spec; the gap is real: Dubai goes silent 8_3→8_5). Design: dropped Campaign 2 (Dubai 8_4 loop); 8_4 drawn as source-suppressed node only; 2 campaigns (Main + AD REMINDER_4 loop). Abu-Dhabi REMINDER_4 is a DIFFERENT, unsuppressed code path and remains a live loop — the two emirates are genuinely asymmetric. Folded into code-interrogator playbook ("Scheduled ≠ actually sent — trace scheduled sends through the dispatch method's runtime gates; flag scheduled-but-suppressed as effectively dead"). Board-quality: hand-positioned CIO board had overlapping nodes + connectors routed through nodes (Moe caught, twice, despite the translator self-reporting clean) → whimsical-standards now mandates auto_layout for the flow core + ORCHESTRATOR (not just drawing agent) verifies the board at zoom before done.
- 2026-07-07 — Replacement-Handover: E4 changed from a new ERP→CIO event to an ATTRIBUTE TRIGGER (Moe). Moe challenged whether E3/E4 needed events given he can extend the sync query to bring old-maid attributes and already has an "unexpected attendance" attr. Verified in code (sessions 39523/39524): (a) E3 (old maid found in attendance) has NO persisted "found" flag — both send sites are fire-in-the-moment → stays an event (source-gated). (b) E4 (returned new maid shows up) DOES have a persisted, per-log, once-per-event flag `HOUSEMAIDATTENDANCELOGS.NEW_MAID_RETURNED` set at `AttendanceStep.replacedButNotReturned:1115` → becomes a `new_maid_returned` sync-add + attribute trigger, no event. CRITICAL trap: do NOT detect E4 via `handOverSelectedMaid=2` — it's set in ~5 paths (incl. client merely choosing the old maid in-app) and overwritten to 1 later → false-pos/neg/double-fire. (c) E2 (missed pickup): Moe's "taxi cancelled/closed + NO_SHOW" doesn't work — CLOSED = picked-up-successfully; CANCELLED is overloaded; the send also has source-side gates (new-maid-taxi-not-CLOSED + template variant) → stays an event. Correction folded: an event is NOT justified by "the entity has no CIO relationship" (the sync query can be extended) — justify events by transient/source-gated/fire-in-the-moment sends; before choosing an event, check for a persisted once-per-event flag an attribute trigger can key on, and never key on an overloaded/mutable field. Applied to board + attribute-map + cio-design (events 4→3, sync-adds 7→8).
- 2026-07-07 — Board-verification process: the flow-diagrammer's "verified clean" self-report missed residual overlaps AGAIN on the Replacement CIO board (Campaign-4 ladder: a +2d G4-diamond 82px overlap + systematic ~5px diamond touches) — Moe caught them. Root cause: visual zoom checks miss thin (≤~10px) overlaps and lower-region density. Rule: the ORCHESTRATOR verifies a board by RECOMPUTING every object's bounding box from a `detailed` fetch and checking for overlapping pairs deterministically, not by eyeballing zoomed images alone. (Already in whimsical-standards that the orchestrator verifies; strengthened here to "by coordinates, not just visually".)
- 2026-07-08 — Board hygiene pass (per Moe): every CIO design board enriched — per-template notes now show **NAME (ID)** bold + "Sends last 45d: N (recipients)" (from broadcasts_final_layer) with the channel tag dropped, plus one green BUILD REQUIREMENTS note (events/attributes/APIs/params) per board. Folded into whimsical-standards as the standing CIO-board format. Overlap audit across all 9 CIO boards → zero overlaps (fixed tall-diamond-over-next-node + note-column stacking that the enrichment tightened). Two "Legacy ERP Boards" subfolders created (MV 9ka6KvrhJC3Zf3L6Te1Ar5, CC 7JuYzEKVAs2qEACt5vQDa7) to separate legacy from CIO boards; existing legacy boards need a manual drag (Whimsical MCP has no move op). Migrate-candidates (live in 2026, not in export) consolidated in work/MV-Clients/MV_MIGRATE_CANDIDATES.md: APPROVE_REDUCE_FINE (+reminders, Cluster 3, was deferred) and CM_SMS_3_3_2_1 (Cluster 14) — export is stale, both are real live branches to consider pulling in.
- 2026-07-08 — Scope rule: the CURRENT MV-Clients JourneyAI export is the authority — migrate ONLY templates in it. Reconciled all completed clusters + candidates against the fresh `templates_targets_MV clients_2026-07-08.xlsx` (246 templates). Removals (live in 2026 but NOT in the new export): 1749 Payroll_Maid_Salary_Transferred (Cluster 13 Campaign A — highest-volume template ~6,976/45d, but absent from the MV export → reclassified to a payroll/other target; Cluster 13 now single-campaign Ansari-only; board Campaign-A flowchart deleted), CM_SMS_3_3_2_1 (Cluster 14 candidate — dropped), APPROVE_REDUCE_FINE_REMINDER_1 (excluded). KEPT (validated present in the new export): the Cluster-3 pulled-ins (PAY_INSIDE 12177, REJECT_REDUCE_FINE 12179, REJECT_R1 12180), Cluster-7 52649, and APPROVE_REDUCE_FINE 12182 + R2 12184 (now an eligible candidate). Two 6-day-apart exports differed (1749 out, 3_3_2_1 absent, APPROVE in) → the export moves; reconcile against the latest before each pass. Recorded in MV_TEMPLATES_REMOVED.md + MV_MIGRATE_CANDIDATES.md.
- 2026-07-08 — System 2 revamp (per Moe, before moving to System 3). (1) The `data-task` stage + `golive-data-team-task-writer` agent are REMOVED — merged into `data-structure`. (2) `golive-data-structure-designer` now consumes System 1's `attribute-map.md` (System 1's attribute-mapper prepares/finalizes the data-attribute steps — added a "data-structure hand-off" to its output), VERIFIES every sync-add attribute / relationship / deletion condition against the ERP code (ask-code) + mmdb, and outputs a code-accurate SPEC of the exact additions to the CIO sync/group queries + the deletion queries. Moe hands that spec to a SEPARATE query-editing session (not a data-team Jira task). (3) `golive-final-checker` gains a second mode: when a CustomerIO MCP is provided (PENDING — not yet available) it inspects the built campaign directly in CIO instead of a pasted description; until then, manual feed. Stages are now dev-task | data-structure | api-spec | final-check. CLAUDE.md System-2 diagram + prepare-golive command + attribute-mapper updated. System 2 is considered done — next is System 3 (live-send monitoring).
- 2026-07-08 — System 3 (shadow-mode reconciliation / go-live parity) BUILT, designed against a PENDING CustomerIO connector (enhance to its real API when Moe confirms it). Cluster goes live in shadow (CIO journeys run, sends suppressed; ERP keeps sending); System 3 compares CIO would-sends vs ERP actual sends (Snowflake broadcasts_final_layer) and adjudicates disagreements + a sample of agreements against DB ground truth (mmdb + low-code). Decisions locked with Moe: accuracy = CIO-correctness vs DB ground truth (ERP fallible → ERP mistakes logged separately, EXCLUDED from CIO's score); per-template rolled to cluster; ~80% ⇒ CUTOVER (CIO sending on, ERP off for that cluster); Step 1 = the connector-upgraded golive-final-checker (entry gate to shadow mode); match = same person + same template + same Dubai calendar day; ground-truth depth = all CIO≠ERP disagreements + a sample of agreements; run on-demand per cluster, trailing ~14d window, ≥~20 would-sends/template to score; honor Snowflake ~1-2h ingestion lag (exclude last ~2h from miss calls, sent_date trusted). Built: docs/system3.md, agent system3-reconciler, command /system3 <target> <cluster> <step> (validate|reconcile), golive-final-checker doubles as Step 1, CLAUDE.md System-3 section. Fix→re-run loop until 80%.
- 2026-08-26 — n8n security remediation, SA-* tickets (Hassan): the Security Room portal is RETIRED, so the fix for the webhook findings is deletion, not defence. The overnight 2026-08-25 approach (shared secret + callback allowlist + CRLF shape checks on every Security Room webhook) is reversed: webhook, respond and callback nodes are DELETED from Payroll (zwSxrV00VE4rOSvd), Client Refunds (NIUelKhaMucLLSqK) and SDR Fee (N3OWVknR68JImzvl); each becomes Monthly Schedule → Run Config (derives the audited month) → existing Validate Inputs → ERP reads via the `ERP Hassan Prod` custom-auth credential (egREvHnZfspVnrza). SA-101/105/129/142 lose their mechanism rather than gaining a guard, and deleting the guards also removed the live rotating shared secret that had spread to thirteen flows. Travel Assist (LM7ofq89VWXiLRU0) was already built this way and is the reference implementation. Standing rules that came out of it: (1) a credential sweep MUST read ACTIVE versions, not just drafts — the live SDR Audit version carried a colleague's ERP JWT on eight nodes while its draft was clean; (2) an attached credential does not mean it is used — those same nodes had `authentication: "none"`, so the credential was inert and the literal header was the real auth path; (3) prefer removing a capability over guarding it when the consumer is gone — SA-116's judge became an Execute Sub-workflow Trigger with callerPolicy `workflowsFromAList`, and both its shared-secret check and its `deterministic_verdicts` opt-in gate were deleted because the gate's only job (telling a sibling from a stranger) is now the trigger's, and keeping it would have thrown on every bundle carrying a PIL red flag; (4) failures must be loud — every `Callback — Error` became `Fail The Run`/failure e-mail, because the old design POSTed failures to the caller-supplied URL, so a run broken by a bad caller was reported only to that caller. Decisions taken with Hassan: payroll's `ansari_data`/`payroll_data` (uploaded spreadsheets, no ERP source) will come from a Google Drive folder — folder id, file-naming rule and the source of `prev_cc_total` still undecided, so `Load Inputs` throws by design rather than silently auditing the wrong month; SA-322's external recipient `3nb3d@3nb3d.com` is intentional (reason still unrecorded); `Daily - Household Expenses` keeps its Wed/Sat/Mon cadence because it is a business report, not a Security Room audit; ZZ SDR Portal Contract Test and the TEMP ERP auth probe archived. NEW finding worth carrying: SA-142 is closed in n8n but REOPENED in Drive — the audit-outputs folder 1DyG9PHws8-52t_vNN96ZAh-T0Ewpoh1w is a shared My Drive folder, not a company Shared Drive (`on_shared_drive: false`, measured three times), so every results sheet is owned by one person and the audit history leaves with them. That is the same OWNERSHIP failure as a personal workers.dev host, and audit-sheet-standard.md §2 already forbids it — the folder does not meet its own standard. Also unfinished and stated as such: the Sheet write is not wired on any rebuilt check, so all three still e-mail their report body, which breaks audit-sheet-standard.md §8 (the e-mail must be a notification carrying only name/window/link). Interim by necessity — shrinking the mail before wiring the sheet would leave findings nowhere. Full detail in docs/n8n-security-remediation.md.
