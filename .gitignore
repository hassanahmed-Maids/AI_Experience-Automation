# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key

# Python
__pycache__/
*.pyc

# Work state (on-disk pipeline state)
work/

# ...but the audit-flow factory's run records ARE deliverables
!audit-flows/work/
!audit-flows/work/**
